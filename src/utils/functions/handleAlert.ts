import { TAlert } from "../../store/reducers/alertReducers"

// eslint-disable-next-line no-empty-pattern
const handleAlert = ({}): TAlert => {
	return {
		type: "success",
		message: "message",
	}
}

export default handleAlert
